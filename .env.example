# Server Configuration
PORT=3000

# Application Base URL (used for generating file download links)
# For local development: http://localhost:3000
# For production: https://yourdomain.com
APP_BASE_URL=http://localhost:3000

# MongoDB Connection URL (REQUIRED)
# Get this from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/dbname?retryWrites=true&w=majority
# See MONGODB_SETUP.md for detailed setup instructions
MONGO_CONNECTION_URL=mongodb+srv://harshadlokare0209_db_user:VUxC6a0uiRuqiFR2@cluster0.feo4xpo.mongodb.net/?appName=Cluster0

# CORS Configuration
# Comma-separated list of allowed origins (e.g., http://localhost:3000,http://localhost:5000)
# Leave empty or set to * to allow all origins
ALLOWED_CLIENTS=http://localhost:3000

# Email Configuration (SMTP) - Optional, only needed for email sharing feature
# SMTP Host (e.g., smtp.gmail.com, smtp.mailtrap.io)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Email Username (your email address)
MAIL_USER=your-email@gmail.com

# Email Password (use app-specific password for Gmail)
MAIL_PASS=your-app-password
